<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Notifications: required so the system asks the user on Android
         13+ (API 33). Before this, schedule() silently fails on
         TIRAMISU+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Exact-time alarms for daily reminders. Without these, repeating
         alarms set via AlarmManager are inexact and Android batches
         them (Doze mode), which can delay an 08:00 reminder by 30-60
         minutes. SCHEDULE_EXACT_ALARM was introduced in Android 12
         (API 31) and requires runtime confirmation; USE_EXACT_ALARM
         was added in Android 13 (API 33) for apps where exact timing
         is core to the feature. MeMap's daily check-in reminder
         qualifies — a notification 45 minutes late silently breaks
         the streak-building habit. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Re-arm scheduled notifications after device reboot. Without
         this the daily reminder silently disappears the first time
         the user restarts their phone. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- @capacitor/haptics declares this in its own manifest and the
         Gradle merger should pull it in, but we restate it here as
         insurance — without VIBRATE the system silently ignores all
         Haptics.impact / .notification calls on every Android version. -->
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
